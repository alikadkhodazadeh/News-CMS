@model Portal.Models.Groups

@{
    ViewBag.Title = "نمایش جزئیات گروه";
}

<h2>@ViewBag.Title</h2>
<hr />
<div>
    <h4>Groups</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("ویرایش", "Edit", new { id = Model.Id }, new { @class = "btn btn-warning" }) |
    @Html.ActionLink("بازگشت به فهرست", "Index", null, new { @class = "btn btn-default" })
</p>
