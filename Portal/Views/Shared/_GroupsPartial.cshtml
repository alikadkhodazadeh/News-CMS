@model IEnumerable<Portal.Models.Groups>

<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">گروه ها <span class="caret"></span></a>
    <ul class="dropdown-menu">
        @foreach (var item in Model)
        {
            <li><a href="/Home/ShowNews/@item.Id">@item.Title</a></li>
        }
    </ul>
</li>